.hlx/*
coverage/*
logs/*
node_modules
.parcel-cache

helix-importer-ui
.DS_Store
*.bak
.idea
# Ignore .map files in scripts/__dropins__/ directory and all its subdirectories
scripts/__dropins__/**/*.map
fstab.yaml
.aio

# Devcontainer per-dev secrets + optional Zscaler cert export
.devcontainer/.env
.devcontainer/zscaler.crt

# Lockfile resolves differently across BAC JFrog / public npm; keep it out of
# git so registry-specific URLs don't leak. Devcontainer regenerates on install.
package-lock.json